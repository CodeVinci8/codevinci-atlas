# tests/synthetic-repo/

Синтетический репозиторий для приёмки конвейера.

**Область VP-2+** (Master Spec §35, §32.3). В VP-0 не используется: доказ-во
handoff строится на детерминированной задаче fake-адаптеров
(`atlas_core.adapters.fake`), а не на реальном diff synthetic-repo.
