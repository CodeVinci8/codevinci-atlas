# pnpm 11: разрешение build-скриптов (supply-chain safety). esbuild (нужен
# Vite) собирает нативный бинарь в postinstall — разрешаем явно и
# неинтерактивно, чтобы pnpm install --frozen-lockfile шёл в CI и Docker.
allowBuilds:
  esbuild: true
