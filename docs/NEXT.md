# NEXT — активный VP и следующий шаг

## Завершено

- **VP-0 — Profile Pool & Live Handoff Proof: ЗАВЕРШЁН, 11/11 PASS**
  ([`vp/VP-0.md`](vp/VP-0.md)). Merged в `main`.
- **VP-1 — Foundation: ЗАВЕРШЁН, 17/17 PASS** ([`vp/VP-1.md`](vp/VP-1.md)).
  Compose Core/Web + systemd Runner + health/migrations/audit + CLI
  `doctor/backup/status` + RU/EN Web-shell + CI. Принимается через PR в `main`.

## Активный VP

**Нет активного VP в разработке.** Следующий по плану — **VP-2 (Project
Workspace, Master Spec §35)**, но он **не начат** (ждёт отдельного решения).

## NEXT_ACTION

Owner ревьюит принятый VP-1 на `main` и стек на `http://127.0.0.1:3210`
(через SSH-туннель). VP-2 начинать только после явного решения. Профильный
пул/маршрутизация и полный Pulse — это VP-5/VP-8, не сейчас.

## Границы

Один активный VP-гейт (WIP=1). VP-2 молча не начинать. LICENSE — отдельное
решение владельца.
